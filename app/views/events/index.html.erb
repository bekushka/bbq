<h1 class="text-center">Добро пожаловать в Ситком про Канатчикову дачу!</h1>

<div class="buffer-both text-center my-5">
  <%= link_to 'Создать событие', new_event_path, class: 'btn btn-info' %>
</div>

<div class="row buffer-bottom">
  <%# Рендерим сразу массив событий, каждый в паршиале 'events/event' %>
  <%= render partial: 'events/event', collection: @events %>
</div>
