<%= form_with(model: usera, local: true) do |form| %>
  <% if usera.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usera.errors.count, "error") %> prohibited this usera from being saved:</h2>

      <ul>
        <% usera.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
